[Definition]
failregex = ^ \d+ INFO \S+ \S+ Login failed for db:\S+ login:\S+ from <HOST>
# Odoo logs in UTC; pin the datepattern so fail2ban never guesses the timestamp.
datepattern = ^%%Y-%%m-%%d %%H:%%M:%%S
ignoreregex =
